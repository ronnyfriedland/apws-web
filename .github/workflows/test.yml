name: Django CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest
    environment: BUILD

    env:
      SECRET_KEY: ${{ vars.SECRET_KEY }}
      DEBUG: ${{ vars.DEBUG }}
      ALLOWED_HOSTS: ${{ vars.ALLOWED_HOSTS }}
      OPENSEARCH_HOST: ${{ vars.OPENSEARCH_HOST }}
      OPENSEARCH_PORT: ${{ vars.OPENSEARCH_PORT }}
      OPENSEARCH_USER: ${{ vars.OPENSEARCH_USER }}
      OPENSEARCH_PASSWORD: ${{ vars.OPENSEARCH_PASSWORD }}
      OPENSEARCH_USE_SSL: ${{ vars.OPENSEARCH_USE_SSL }}
      OPENSEARCH_SSL_VERIFY: ${{ vars.OPENSEARCH_SSL_VERIFY }}
      CAMERA_HOST: ${{ vars.CAMERA_HOST }}
      CAMERA_PORT: ${{ vars.CAMERA_PORT }}

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: 3.13
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade uv
        uv sync --frozen
    - name: Run Tests
      run: |
        cd apws
        uv run python3 manage.py test
